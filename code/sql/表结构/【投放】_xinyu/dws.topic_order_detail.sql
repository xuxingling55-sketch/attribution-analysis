-- =====================================================
-- 文档说明：本文件为 dws.topic_order_detail 表级文档（SPEC 三段式）。
-- 无数仓 CREATE 快照时仅注释；以数仓实际为准。
-- =====================================================
-- 全公司订单宽表 dws.topic_order_detail
-- =====================================================
-- 【表粒度】
--   一个子订单一条；T+1
--
-- 【使用场景】
--   - 判断用户是否买过某商品（必须用本表，避免漏多渠道）
--   - 各业务 GMV：business_gmv_attribution
--   - 服务期营收：team_names
--   - 与活跃表联：u_user
--
-- 【业务定位】
--   全公司订单宽表；与电销专用 aws.crm_order_info 区分，见 glossary「选表」
--
-- 【统计口径】
--   营收：SUM(order_amount) 订单去重 或 SUM(sub_amount) 子订单粒度；需 is_test_user = 0
--   正价：order_amount >= 39
--   统计ltv时使用字段到账金额：SUM(arrival_amount)，订单状态 status in ('支付成功','退款成功')
--
-- 【常用筛选条件】
--   ★必加：
--   - is_test_user = 0
--   场景追加：
--   - 看营收：status = '支付成功'
--   - 看 LTV：status IN ('支付成功','退款成功')
--   - 正价：order_amount >= 39
--
-- 【注意事项】
--   通用规则 R02/R03 见 glossary.md
-- =====================================================
--
-- （无数仓 CREATE TABLE 快照）
--
-- =====================================================
-- 枚举值（节选）
-- =====================================================
--
-- ## status（常用）
-- | 值 | 含义 |
-- |----|------|
-- | 支付成功 | 已支付 |
-- | 退款成功 | 已退款 |
--
-- ## good_kind_name_level_2（部分）
-- 大会员、组合商品、同步课等，完整见 glossary 商品章节
--
-- ## user_pay_status_* / business_user_pay_status_*
-- 见 glossary.md「用户分层」
