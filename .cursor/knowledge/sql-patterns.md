# 标准 SQL 参考库

> 模板须可直接执行；参数统一用 `${param}`；口径需与 `glossary/` 对应域文件一致。

## 模板索引

| 编号 | 场景 | 参考 DDL / 说明 |
|------|------|-----------------|
| P-001 | （待补） | → `code/sql/表结构/【宽表】_xingling/` |

---

## P-001 {模板标题}（骨架）

**参数**：`${day_start}`、`${day_end}` …

```sql
-- 待补：可执行模板
SELECT 1;
```

**参考**：`code/sql/表结构/【宽表】_xingling/dws.topic_order_detail.sql`（示例路径，按实际替换）

---

## 更新记录

| 日期 | 内容 |
|------|------|
| 2026-04-01 | 建立骨架 |
